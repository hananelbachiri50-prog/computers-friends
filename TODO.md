# TODO

- [x] Mettre à jour `resources/views/welcome.blade.php` pour que chaque produit ait son propre album GLightbox (mapping p1..p4) via `data-gallery="gallery-{{ $product->id }}"`.
- [x] Vérifier que le bouton “Plus d'infos” déclenche la Lightbox et que les images supplémentaires sont rendues en `<a class="glightbox" style="display:none">`.
- [x] Ne pas modifier le CSS existant (fond noir / accents dorés).

- [ ] Test manuel : ouvrir la page d’accueil, vérifier la navigation (flèches) et la fermeture de la Lightbox pour chaque produit.

